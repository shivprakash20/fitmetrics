# FitMetrics Mobile (Expo)

This app consumes the backend exposed by `apps/web`.

## Run

```bash
npm run dev:mobile
```

## Next steps

- Add API client with shared contracts from `packages/contracts`
- Implement secure token storage (`expo-secure-store`)
- Wire login/profile screens with `/api/v1/auth/mobile/*` and `/api/v1/profile`
